import * as React from 'react';

export interface InterpretationPageProps { }

export class InterpretationPage extends React.Component<InterpretationPageProps, {}> {
    render() {
        return (
            <h1>Interpretation Page</h1>
        );
    }
}