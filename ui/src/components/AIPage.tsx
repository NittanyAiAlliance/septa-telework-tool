import * as React from 'react';
import Container from "react-bootstrap/Container";

export interface AIPageProps { }

export class AIPage extends React.Component<AIPageProps, {}> {
    render() {
        return (
            <h1>AI page</h1>
        );
    }
}